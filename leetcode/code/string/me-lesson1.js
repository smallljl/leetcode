// export default (str)=>{
//     return str.length ? str.match(/[\S]+/g).map(item=>{
//         return item.split("").reverse().join("");
//     }).join(' ') : '';
// }

// export default (str)=>{
//     return str.length ? str.match(/[\S]+/g).map(item=>{
//         return item.split("").reverse().join("");
//     }).join(" ") : '';
// }

// export default (str)=>{
//     return str.length ? str.match(/[\S]+/g).map(item=>{
//         return item.split("").reverse().join("");
//     }).join(" ") : "";
// }

// export default (str)=>{
//     return str.length ? str.match(/[\S]+/g).map(item=>{
//         return item.split("").reverse().join("");
//     }).join(" ") : '';
// }

// export default (str)=>{
//     return str.length ? str.match(/[\S]+/g).map(item=>{
//         return item.split("").reverse().join("");
//     }).join(" ") : '';
// }

// export default (str)=>{
//     return str.length ? str.match(/[\S]+/g).map(item=>{
//         return item.split("").reverse().join("");
//     }).join(" ") :""
// }

// export default (str) => {
//     return str.length ? str.match(/[\S]+/g).map(item=>{
//         return item.split("").reverse().join("");
//     }).join(" ") : "";
// }

// export default (str)=>{
//     return str.length ?  str.match(/[\S]+/g).map(item=>{
//         return item.split("").reverse().join("");
//     }).join(" "): ''
// }

// export default (str)=>{
//     return str.length ? str.match(/[\S]+/g).map(item=>{
//         return item.split("").reverse().join("");
//     }).join(" ") : ""
// }